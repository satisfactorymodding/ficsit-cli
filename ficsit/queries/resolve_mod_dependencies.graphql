query ResolveModDependencies ($filter: [ModVersionConstraint!]!) {
    mods: resolveModVersions(filter: $filter) {
        id
        mod_reference
        versions {
            id
            version
            targets {
                targetName
                link
                hash
            }
            dependencies {
                condition
                mod_id
                optional
            }
        }
    }
}