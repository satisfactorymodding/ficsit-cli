# @genqlient(omitempty: true)
query Mods ($filter: ModFilter) {
    getMods (filter: $filter) {
        count
        mods {
            id
            name
            mod_reference
        }
    }
}
